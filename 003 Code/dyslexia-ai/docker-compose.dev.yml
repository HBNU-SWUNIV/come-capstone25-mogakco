services:
  app:
    # Dev overrides: bind-mount source and enable reload
    volumes:
      - ./:/app
      - ./temp:/app/temp
      - ./output:/app/output
    command: ["uvicorn", "main:app", "--reload", "--host", "0.0.0.0", "--port", "10300"]
